# FT.com Configuration (Legacy Syntax)
# We use 'header:' instead of 'http_header:' for the free version.

header: User-Agent: curl/7.68.0
header: Accept: */*
header: Cookie: PASTE_YOUR_CLEAN_COOKIE_HERE

normalize_url: yes

# Extraction Rules (Core/No-JS Support)
# We cast a wide net to catch whatever HTML layout FT sends
body: //div[contains(@class, 'article-body')]
body: //div[contains(@class, 'article__content-body')]
body: //div[@id='site-content']
body: //main
body: //article

# Cleanup
strip: //div[contains(@class,'barrier')]
strip: //div[contains(@data-component,'offer-card')]
strip: //aside
strip: //footer
strip: //nav
strip: //div[contains(@class,'ad')]
strip: //div[contains(@class,'o-ads')]

prune: yes
tidy: yes
